aws ssm create-activation \
    --default-instance-name "A4L-ONPREM" \
    --iam-role "HybridSSMServiceRole" \
    ---expiration-date "2021-07-07T00:00:00" \
    --registration-limit 10


aws iam create-role --role-name HybridSSMServiceRole --assume-role-policy-document file://SSMService-Trust.json

aws iam delete-role --role-name HybridSSMServiceRole
